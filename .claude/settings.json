{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"/Users/dator/Documents/DEV1/saas shop\" && git add -u && if ! git diff --cached --quiet; then git commit -m \"chore: auto-save $(date +'%Y-%m-%d %H:%M')\" && git push origin HEAD; fi",
            "timeout": 30,
            "statusMessage": "Pushing changes to GitHub..."
          }
        ]
      }
    ]
  }
}
